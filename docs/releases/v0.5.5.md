# v0.5.5 - goneat v0.6.0 runner pin

**Date:** 2026-09-01

**Type:** Patch (single tool pin; no image contract change)

## Summary

Rebuilds the goneat-tools images with **goneat v0.6.0**. The pin moves in lockstep across both Dockerfiles and `manifests/tools.json`. All other tool, OS-package, and base-image pins remain unchanged from v0.5.4.

## Changes

1. **`goneat` `v0.5.16` -> `v0.6.0`** on `goneat-tools-slim-musl` and `goneat-tools-runner-{musl,glibc}`.
2. **Release pack** - `VERSION` 0.5.5, release notes, changelog, and this release document agree.

## Non-goals (this cut)

- Tool updates other than goneat.
- OS-package or base-image refreshes.
- Release workflow changes.
- Image layout or command-path changes.

## Upgrade notes

Consumers tracking `:v0.5.4` (or a digest from that tag) should retag to `:v0.5.5` to pick up goneat v0.6.0. Floating `:v0` / `:latest` move on GA.

## Verification checklist

- [ ] `GONEAT_VERSION` agrees across both Dockerfiles and `manifests/tools.json`.
- [ ] `make pr-final` passes and leaves the intended worktree diff only.
- [ ] `make validate-licenses` passes.
- [ ] `VERSION` = 0.5.5; `RELEASE_NOTES.md`, `CHANGELOG.md`, and this file agree with `manifests/tools.json`.
- [ ] Tag `v0.5.5` -> release workflow green; multiarch + cosign + checksums; `:latest` / `:v0` follow GA.
