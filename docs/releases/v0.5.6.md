# v0.5.6 - goneat v0.6.0 runner pin + musl jq 1.8.2

**Date:** 2026-09-02

**Type:** Patch (tool pins; no image contract change)

## Summary

Rebuilds the goneat-tools images with **goneat v0.6.0**. The pin moves in lockstep across both Dockerfiles and `manifests/tools.json`. The musl images also move to **jq 1.8.2-r0** after Alpine 3.24 rotated `1.8.1-r0` out of its repositories. All other tool, OS-package, and base-image pins remain unchanged from v0.5.4.

v0.5.5 was not released. Its workflow failed before manifest publication, and this release supersedes it.

## Changes

1. **`goneat` `v0.5.16` -> `v0.6.0`** on `goneat-tools-slim-musl` and `goneat-tools-runner-{musl,glibc}`.
2. **musl `jq` `1.8.1-r0` -> `1.8.2-r0`** on `goneat-tools-slim-musl` and `goneat-tools-runner-musl`.
3. **Release pack** - `VERSION` 0.5.6, release notes, changelog, and this release document agree.

## Non-goals (this cut)

- Tool updates other than goneat and musl jq.
- OS-package or base-image refreshes.
- Release workflow changes.
- Image layout or command-path changes.

## Upgrade notes

Consumers tracking `:v0.5.4` (or a digest from that tag) should retag to `:v0.5.6` to pick up goneat v0.6.0 and musl jq 1.8.2. Floating `:v0` / `:latest` move on GA.

## Verification checklist

- [ ] `GONEAT_VERSION` agrees across both Dockerfiles and `manifests/tools.json`.
- [ ] musl `JQ_VERSION` agrees with `manifests/tools.json` and validates against Alpine repositories.
- [ ] `make pr-final` passes and leaves the intended worktree diff only.
- [ ] `make validate-licenses` passes.
- [ ] `VERSION` = 0.5.6; `RELEASE_NOTES.md`, `CHANGELOG.md`, and this file agree with `manifests/tools.json`.
- [ ] Tag `v0.5.6` -> release workflow green; multiarch + cosign + checksums; `:latest` / `:v0` follow GA.
