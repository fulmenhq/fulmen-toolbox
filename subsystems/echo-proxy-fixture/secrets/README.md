# Secrets

This subsystem does not require secrets by default.

If you add optional configuration that requires secrets, place them in this directory.

- This directory is structured to ignore all secret material in git.
- Only `.gitignore` and this `README.md` should be committed.
